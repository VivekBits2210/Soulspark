# TODO: Orchestrate flow via generate_reply
class DialogueEngine:
    def __init__(self, user, bot):
        self.user = user
        self.bot = bot

    def generate_reply(self, message):
        return "This is a canned message"
